# Binaries
/otto
/otto-memory
*.exe
*.test
*.out

# Local config / secrets — never commit
/config.toml
/client_secret.json
/google_token.json
/mcp.json

# Build artifacts
/dist/
/coverage.out

# Claude Code runtime state (locks, scheduled tasks, transcripts)
.claude/
